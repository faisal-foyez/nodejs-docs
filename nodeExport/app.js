// const items = require('./car');

// process.stdout.write(JSON.stringify(items.car));

// const car = require('./car').car;

// process.stdout.write(JSON.stringify(car));

const {car} = require('./car');

process.stdout.write(JSON.stringify(car));